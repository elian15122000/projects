public class StringInInteger {
    public static Integer zahl(String zahl) {
        int zahlAlsInteger = Integer.parseInt(zahl);  // eingegebenen String in Integer konvertieren
        return zahlAlsInteger;   // neuen Integer als Rückgabewert
    }
}
